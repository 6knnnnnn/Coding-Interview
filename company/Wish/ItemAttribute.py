from collections import defaultdict

def findSameAttributes(attributes1, attributes2):
    res = []

def generateAttributeKey(attributes, k):
    # select k from attributes as a composite key
    keys = []


def classifyItems(inputs):
    attrItemMap = defaultdict(set)
    for i in inputs:
        item, attributes = i[0], i[1:]



